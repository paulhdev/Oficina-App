import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        backgroundColor: '#fff'
    },
    header: {
        fontSize: 25,
        color: '#444',
        padding: 10,
        borderBottomWidth: 1,
        borderColor: '#444'
    }
})